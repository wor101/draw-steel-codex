--- @class StoreCouponEntry Represents a store coupon entry, including its item, redemption status, and associated metadata.
--- @field mtime number The redemption timestamp as a number (alias for redeemTime).
--- @field ctime number The creation timestamp as a number.
StoreCouponEntry = {}
