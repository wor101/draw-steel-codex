--- @class effectsBrush Provides an interface for building and previewing effects brush synthesis, which generates a composite brush from multiple object types.
effectsBrush = {}

--- BuildBrush: Builds and renders a brush preview from the given configuration table containing 'objects' (list of SynthesizedObjectLua) and 'options' (EffectsBrushSynthesisOptionsLua).
--- @param info table A table with 'objects' (list of object definitions) and 'options' (synthesis options).
function effectsBrush:BuildBrush(info)
	-- dummy implementation for documentation purposes only
end
